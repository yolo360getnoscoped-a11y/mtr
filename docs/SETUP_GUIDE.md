# 📘 คู่มือการรันโปรเจกต์แบบละเอียด
## ระบบบริหารจัดการการเรียน - ระบบเช็คชื่อด้วย QR Code

---

## 📋 สารบัญ
1. [ความต้องการของระบบ](#ความต้องการของระบบ)
2. [การติดตั้งครั้งแรก](#การติดตั้งครั้งแรก)
3. [การรันโปรเจกต์ทุกครั้ง](#การรันโปรเจกต์ทุกครั้ง)
4. [การเข้าถึงจากมือถือ (ngrok)](#การเข้าถึงจากมือถือ-ngrok)
5. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## 🔧 ความต้องการของระบบ

### Software ที่ต้องติดตั้ง:
- **Python 3.8+** - [ดาวน์โหลดที่นี่](https://www.python.org/downloads/)
- **PostgreSQL 12+** - [ดาวน์โหลดที่นี่](https://www.postgresql.org/download/)
- **Git** (ถ้าต้องการ clone) - [ดาวน์โหลดที่นี่](https://git-scm.com/downloads)

### ตรวจสอบการติดตั้ง:
```powershell
# ตรวจสอบ Python
python --version

# ตรวจสอบ pip
pip --version

# ตรวจสอบ PostgreSQL
psql --version
```

---

## 🚀 การติดตั้งครั้งแรก

### ขั้นตอนที่ 1: เตรียมโปรเจกต์

1. **เปิด PowerShell หรือ Command Prompt**
2. **ไปที่โฟลเดอร์โปรเจกต์:**
   ```powershell
   cd C:\Users\yolo3\Downloads\mtr-20251105T112108Z-1-001\mtr
   ```

### ขั้นตอนที่ 2: สร้าง Virtual Environment

```powershell
# สร้าง virtual environment
python -m venv venv

# เปิดใช้งาน virtual environment (Windows)
.\venv\Scripts\activate

# ตรวจสอบว่าเปิดใช้งานแล้ว (จะเห็น (venv) ด้านหน้า)
```

### ขั้นตอนที่ 3: ติดตั้ง Dependencies

```powershell
# อัปเดต pip
python -m pip install --upgrade pip

# ติดตั้ง packages ทั้งหมด
pip install -r requirements.txt
```

**Packages ที่จะติดตั้ง:**
- Django>=4.2.0
- psycopg2-binary>=2.9.0
- qrcode[pil]>=7.4.0
- Pillow>=10.0.0
- python-decouple>=3.8
- openpyxl>=3.1.0

### ขั้นตอนที่ 4: ตั้งค่า PostgreSQL Database

1. **เปิด pgAdmin หรือ psql**

2. **สร้างฐานข้อมูล:**
   ```sql
   CREATE DATABASE checkin_db;
   ```

3. **สร้าง User (ถ้ายังไม่มี):**
   ```sql
   CREATE USER postgres WITH PASSWORD 'postgres';
   GRANT ALL PRIVILEGES ON DATABASE checkin_db TO postgres;
   ```

   **หมายเหตุ:** ถ้าใช้ user `postgres` ที่มีอยู่แล้ว ให้ข้ามขั้นตอนนี้

### ขั้นตอนที่ 5: ตั้งค่าไฟล์ .env

1. **สร้างไฟล์ `.env` ในโฟลเดอร์ root ของโปรเจกต์**

2. **เพิ่มเนื้อหาดังนี้:**
   ```
   SECRET_KEY=django-insecure-change-this-in-production
   DEBUG=True
   ALLOWED_HOSTS=localhost,127.0.0.1,*
   DB_NAME=checkin_db
   DB_USER=postgres
   DB_PASSWORD=postgres
   DB_HOST=localhost
   DB_PORT=5432
   ```

   **หมายเหตุ:** 
   - แก้ไข `DB_PASSWORD` ให้ตรงกับรหัสผ่าน PostgreSQL ของคุณ
   - แก้ไข `DB_USER` ถ้าใช้ user อื่น

### ขั้นตอนที่ 6: รัน Migrations

```powershell
# สร้าง migration files (ถ้ายังไม่มี)
python manage.py makemigrations

# รัน migrations เพื่อสร้างตารางในฐานข้อมูล
python manage.py migrate
```

**ผลลัพธ์ที่คาดหวัง:**
- จะเห็นข้อความว่า "Applying ... OK" สำหรับแต่ละ migration
- ตารางทั้งหมดจะถูกสร้างในฐานข้อมูล `checkin_db`

### ขั้นตอนที่ 7: สร้าง Superuser (Admin)

```powershell
python manage.py createsuperuser
```

**กรอกข้อมูล:**
- Username: `admin` (หรือชื่อที่ต้องการ)
- Email: `admin@example.com` (หรือเว้นว่าง)
- Password: `admin123` (หรือรหัสที่ต้องการ)
- Password (again): ยืนยันรหัสผ่าน

### ขั้นตอนที่ 8: (Optional) สร้างข้อมูลตัวอย่าง

```powershell
python setup_initial_data.py
```

**ข้อมูลที่สร้าง:**
- ปีการศึกษา 2567
- ภาคเรียนที่ 1 และ 2
- รายวิชา 4 วิชา (CS101, CS201, CS301, CS302)
- อาจารย์ 2 คน (teacher1, teacher2)
- นักศึกษา 5 คน (student1-5)
- กลุ่มเรียนและลงทะเบียน

**บัญชีตัวอย่าง:**
- Teacher: `teacher1` / `teacher123`
- Student: `student1` / `student123`

---

## 🏃 การรันโปรเจกต์ทุกครั้ง

### ขั้นตอนที่ 1: เปิด Virtual Environment

```powershell
# ไปที่โฟลเดอร์โปรเจกต์
cd C:\Users\yolo3\Downloads\mtr-20251105T112108Z-1-001\mtr

# เปิดใช้งาน virtual environment
.\venv\Scripts\activate
```

### ขั้นตอนที่ 2: ตรวจสอบว่า PostgreSQL ทำงานอยู่

```powershell
# ตรวจสอบ service
Get-Service postgresql*
```

**ถ้าไม่ทำงาน:**
```powershell
# เริ่ม service (ต้องใช้สิทธิ์ Administrator)
Start-Service postgresql-x64-14  # แก้ไขเลขเวอร์ชันให้ตรงกับของคุณ
```

### ขั้นตอนที่ 3: รัน Django Development Server

```powershell
# รัน server สำหรับเข้าถึงจากคอมพิวเตอร์เท่านั้น
python manage.py runserver

# หรือรัน server สำหรับเข้าถึงจากเครือข่าย (LAN)
python manage.py runserver 0.0.0.0:8000
```

**ผลลัพธ์ที่คาดหวัง:**
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

### ขั้นตอนที่ 4: เปิดเบราว์เซอร์

- **เข้าถึงจากคอมพิวเตอร์:** http://127.0.0.1:8000
- **เข้าถึงจากเครือข่าย:** http://[IP_ADDRESS]:8000
  - หา IP Address: `ipconfig` → ดู `IPv4 Address` ของ Wi-Fi

### ขั้นตอนที่ 5: หยุด Server

กด `Ctrl+C` ในหน้าต่าง PowerShell ที่รัน server

---

## 📱 การเข้าถึงจากมือถือ (ngrok)

### ขั้นตอนที่ 1: ดาวน์โหลดและติดตั้ง ngrok

**ถ้ายังไม่มี ngrok:**

```powershell
# ไปที่โฟลเดอร์โปรเจกต์
cd C:\Users\yolo3\Downloads\mtr-20251105T112108Z-1-001\mtr

# ดาวน์โหลด ngrok
Invoke-WebRequest -Uri https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip -OutFile ngrok.zip

# แตกไฟล์
Expand-Archive -Path ngrok.zip -DestinationPath . -Force

# ลบไฟล์ zip
Remove-Item ngrok.zip
```

### ขั้นตอนที่ 2: ตั้งค่า ngrok Authtoken

1. **สมัครบัญชี ngrok (ฟรี):**
   - ไปที่ https://dashboard.ngrok.com/signup

2. **หา Authtoken:**
   - ไปที่ https://dashboard.ngrok.com/get-started/your-authtoken
   - คัดลอก token

3. **ตั้งค่า token:**
   ```powershell
   .\ngrok.exe authtoken YOUR_AUTHTOKEN_HERE
   ```

### ขั้นตอนที่ 3: รัน ngrok

**เปิดหน้าต่าง PowerShell ใหม่** (ให้ Django server ยังรันอยู่):

```powershell
# ไปที่โฟลเดอร์โปรเจกต์
cd C:\Users\yolo3\Downloads\mtr-20251105T112108Z-1-001\mtr

# รัน ngrok
.\ngrok.exe http 8000 --host-header="localhost:8000"
```

**ผลลัพธ์ที่คาดหวัง:**
```
ngrok                                                                        

Session Status                online
Account                       [your-email] (Plan: Free)
Version                       3.x.x
Region                        United States (us)
Latency                       45ms
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://xxxx-xxxx-xxxx.ngrok-free.dev -> http://localhost:8000

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

**คัดลอก URL:** `https://xxxx-xxxx-xxxx.ngrok-free.dev`

### ขั้นตอนที่ 4: อัปเดต CSRF_TRUSTED_ORIGINS

**ถ้า ngrok URL เปลี่ยน** ให้แก้ไข `checkin_project/settings.py`:

```python
CSRF_TRUSTED_ORIGINS = [
    'https://YOUR_NEW_NGROK_URL.ngrok-free.dev',  # เพิ่ม URL ใหม่
    'https://*.ngrok-free.dev',
    'https://*.ngrok.io',
    'http://localhost:8000',
    'http://127.0.0.1:8000',
]
```

**รีสตาร์ท Django server** หลังจากแก้ไข

### ขั้นตอนที่ 5: เปิดไฟร์วอลล์ Windows (ถ้าจำเป็น)

```powershell
# เปิด PowerShell แบบ Administrator
netsh advfirewall firewall add rule name="Django Development Server" dir=in action=allow protocol=TCP localport=8000
```

### ขั้นตอนที่ 6: เข้าถึงจากมือถือ

1. **เปิดเบราว์เซอร์บนมือถือ** (Chrome หรือ Safari)
2. **ไปที่ URL:** `https://xxxx-xxxx-xxxx.ngrok-free.dev`
3. **ล็อกอินและใช้งาน**

**หมายเหตุ:** 
- ต้องใช้ HTTPS URL จาก ngrok (ไม่ใช่ HTTP)
- กล้องจะทำงานได้เฉพาะผ่าน HTTPS

### ขั้นตอนที่ 7: หยุด ngrok

กด `Ctrl+C` ในหน้าต่าง PowerShell ที่รัน ngrok

---

## 🔍 การแก้ไขปัญหา

### ปัญหา: ไม่สามารถเชื่อมต่อฐานข้อมูลได้

**สาเหตุ:** PostgreSQL ไม่ทำงานหรือข้อมูลใน `.env` ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบว่า PostgreSQL service ทำงานอยู่
2. ตรวจสอบข้อมูลใน `.env` (DB_NAME, DB_USER, DB_PASSWORD, DB_HOST, DB_PORT)
3. ทดสอบเชื่อมต่อด้วย psql:
   ```powershell
   psql -U postgres -d checkin_db
   ```

### ปัญหา: CSRF verification failed

**สาเหตุ:** ngrok URL ไม่ได้อยู่ใน `CSRF_TRUSTED_ORIGINS`

**วิธีแก้:**
1. ตรวจสอบ ngrok URL ปัจจุบัน
2. เพิ่ม URL ใน `checkin_project/settings.py` → `CSRF_TRUSTED_ORIGINS`
3. รีสตาร์ท Django server

### ปัญหา: ไม่สามารถเข้าถึงกล้องได้

**สาเหตุ:** 
- ใช้ HTTP แทน HTTPS
- เบราว์เซอร์ไม่รองรับ
- ไม่ได้อนุญาตสิทธิ์กล้อง

**วิธีแก้:**
1. ใช้ ngrok HTTPS URL (ไม่ใช่ HTTP)
2. ใช้ Chrome หรือ Safari เวอร์ชันล่าสุด
3. อนุญาตสิทธิ์กล้องเมื่อเบราว์เซอร์ถาม
4. ตรวจสอบการตั้งค่าสิทธิ์ในเบราว์เซอร์

### ปัญหา: Port 8000 ถูกใช้งานแล้ว

**สาเหตุ:** มี Django server รันอยู่แล้ว

**วิธีแก้:**
```powershell
# หา process ที่ใช้พอร์ต 8000
netstat -ano | findstr :8000

# หยุด process (ใช้ PID จากคำสั่งด้านบน)
taskkill /F /PID [PID_NUMBER]
```

### ปัญหา: ngrok version too old

**สาเหตุ:** ใช้ ngrok เวอร์ชันเก่า

**วิธีแก้:**
1. ลบ ngrok.exe เก่า
2. ดาวน์โหลด ngrok v3 ใหม่ (ตามขั้นตอนที่ 1)
3. ตั้งค่า authtoken ใหม่

---

## 📝 สรุปขั้นตอนการรัน (Quick Start)

### สำหรับการใช้งานปกติ (คอมพิวเตอร์):

```powershell
# 1. เปิด virtual environment
cd C:\Users\yolo3\Downloads\mtr-20251105T112108Z-1-001\mtr
.\venv\Scripts\activate

# 2. รัน server
python manage.py runserver

# 3. เปิดเบราว์เซอร์: http://127.0.0.1:8000
```

### สำหรับการใช้งานจากมือถือ:

**Terminal 1 - Django Server:**
```powershell
cd C:\Users\yolo3\Downloads\mtr-20251105T112108Z-1-001\mtr
.\venv\Scripts\activate
python manage.py runserver 0.0.0.0:8000
```

**Terminal 2 - ngrok:**
```powershell
cd C:\Users\yolo3\Downloads\mtr-20251105T112108Z-1-001\mtr
.\ngrok.exe http 8000 --host-header="localhost:8000"
```

**ใช้ URL จาก ngrok บนมือถือ**

---

## 📞 การติดต่อ

หากมีปัญหาหรือคำถาม:
- ติดต่อ: 090-895-4126
- หรือเปิด Issue ใน GitHub repository

---

**อัปเดตล่าสุด:** 2025-11-11  
**เวอร์ชัน:** 1.0

