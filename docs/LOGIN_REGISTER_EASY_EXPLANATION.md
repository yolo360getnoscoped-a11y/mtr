# 📖 อธิบายการทำงานของหน้า Login และ Register แบบง่ายๆ (เข้าใจง่าย)

## 🎯 สารบัญ
1. [หน้า Login (เข้าสู่ระบบ)](#หน้า-login)
2. [หน้า Register (สมัครสมาชิก)](#หน้า-register)
3. [ฐานข้อมูลทำงานยังไง](#ฐานข้อมูลทำงานยังไง)

---

## 🔐 หน้า Login (เข้าสู่ระบบ)

### 📍 โค้ดอยู่ที่ไหน?

| ส่วน | โฟลเดอร์ | ไฟล์ | บรรทัด | อธิบาย |
|------|---------|------|--------|--------|
| Template | `templates/accounts/` | `login.html` | 1-127 | หน้าเว็บที่ user เห็น |
| View Function | `accounts/` | `views.py` | 29-59 | โค้ดที่ประมวลผล |
| URL Config | `accounts/` | `urls.py` | 11 | เชื่อม URL กับ function |

---

### 🎨 หน้าจอ Login ทำงานยังไง? (แบบง่าย)

**เหมือนกับเราไปร้านหนังสือ:**
1. คุณเดินเข้าไป (เข้า URL: `/login/`)
2. คุณเห็นฟอร์มให้กรอก (หน้าเว็บแสดง)
3. คุณกรอกชื่อผู้ใช้และรหัสผ่าน (กรอกข้อมูล)
4. คุณส่งฟอร์มให้พนักงาน (กดปุ่ม "เข้าสู่ระบบ")
5. พนักงานเช็คว่าคุณเป็นสมาชิกหรือเปล่า (ระบบเช็คในฐานข้อมูล)
6. ถ้าใช่ → ให้เข้าไปได้ (เข้าสู่ระบบสำเร็จ)
7. ถ้าไม่ใช่ → บอกว่าไม่มีชื่อนี้ (แสดงข้อความ error)

---

### 📝 ขั้นตอนการทำงาน (แบบละเอียด)

#### **ขั้นที่ 1: User เข้าหน้า Login**

📁 **โค้ด:** `templates/accounts/login.html` บรรทัด **1-127**

```
User พิมพ์: http://127.0.0.1:8000/login/
    ↓
Browser ส่ง request ไปยัง Server
    ↓
Server ตรวจสอบว่า user login อยู่แล้วหรือยัง?
```

**โค้ดที่ตรวจสอบ:**
```python
# accounts/views.py บรรทัด 34-35
if request.user.is_authenticated:
    return redirect('accounts:profile')  # ถ้า login อยู่แล้ว → ไปหน้า profile
```

---

#### **ขั้นที่ 2: แสดงหน้า Login**

📁 **โค้ด:** `templates/accounts/login.html` บรรทัด **76-100**

**HTML ที่แสดงบนหน้าจอ:**
```html
<!-- บรรทัด 76: เริ่มฟอร์ม -->
<form method="post" action="{% url 'accounts:login' %}">
    
    <!-- บรรทัด 77: CSRF Token (เหมือนกุญแจพิเศษป้องกันคนโกง) -->
    {% csrf_token %}
    
    <!-- บรรทัด 84: ช่องกรอกชื่อผู้ใช้ -->
    <input type="text" name="username" placeholder="กรอกชื่อผู้ใช้">
    
    <!-- บรรทัด 93: ช่องกรอกรหัสผ่าน -->
    <input type="password" name="password" placeholder="กรอกรหัสผ่าน">
    
    <!-- บรรทัด 99: ปุ่มกดเพื่อส่งข้อมูล -->
    <button type="submit">เข้าสู่ระบบ</button>
</form>
```

**อธิบายแบบง่าย:**
- `<form>` = กล่องที่ใส่ช่องกรอกข้อมูล
- `method="post"` = ส่งข้อมูลแบบลับ (ไม่ให้เห็นใน URL)
- `name="username"` = ชื่อของข้อมูลที่ส่งไป
- `type="password"` = พิมพ์แล้วจะเป็นจุดๆ (●●●●) เพื่อไม่ให้คนอื่นเห็น

---

#### **ขั้นที่ 3: User กรอกข้อมูลแล้วกด Submit**

📁 **โค้ด:** `accounts/views.py` บรรทัด **37-39**

**เมื่อ user กดปุ่ม "เข้าสู่ระบบ":**
```python
# บรรทัด 37: ตรวจสอบว่าส่งข้อมูลแบบ POST มาไหม
if request.method == 'POST':
    
    # บรรทัด 38: ดึงชื่อผู้ใช้ที่กรอกมา
    username = request.POST.get('username')
    
    # บรรทัด 39: ดึงรหัสผ่านที่กรอกมา
    password = request.POST.get('password')
```

**อธิบายแบบง่าย:**
- `request.POST.get('username')` = ไปดูในกล่องที่ชื่อ "username" แล้วเอาค่ามา
- เหมือนถามว่า "คุณกรอกชื่ออะไร?"

---

#### **ขั้นที่ 4: ตรวจสอบว่ากรอกข้อมูลครบไหม**

📁 **โค้ด:** `accounts/views.py` บรรทัด **41, 56-57**

```python
# บรรทัด 41: เช็คว่ามี username และ password หรือเปล่า
if username and password:
    # ถ้ามี → ไปขั้นตอนต่อไป
else:
    # บรรทัด 57: ถ้าไม่มี → บอกให้กรอกข้อมูล
    messages.error(request, 'กรุณากรอกชื่อผู้ใช้และรหัสผ่าน')
```

**อธิบายแบบง่าย:**
- เช็คว่าคุณกรอกข้อมูลมาครบหรือยัง
- ถ้ายังไม่ครบ → บอกให้กรอกใหม่

---

#### **ขั้นที่ 5: เช็คว่า username และ password ถูกต้องไหม**

📁 **โค้ด:** `accounts/views.py` บรรทัด **42**

```python
# บรรทัด 42: เช็คว่า username และ password ตรงกับฐานข้อมูลหรือไม่
user = authenticate(request, username=username, password=password)
```

**อธิบายแบบง่าย:**
- `authenticate()` = ฟังก์ชันที่ไปเช็คในฐานข้อมูล
- ไปหาว่ามี username นี้ไหม?
- ถ้ามี → เช็คว่า password ถูกไหม?
- ถ้าถูก → return User object
- ถ้าไม่ถูก → return None (ไม่มีอะไร)

**ฐานข้อมูลทำงานยังไง:**
```sql
-- ระบบจะ query ฐานข้อมูลแบบนี้:
SELECT * FROM accounts_user 
WHERE username = 'admin' 
AND is_active = TRUE;
```

---

#### **ขั้นที่ 6: ถ้าถูกต้อง → เข้าสู่ระบบ**

📁 **โค้ด:** `accounts/views.py` บรรทัด **43-45**

```python
# บรรทัด 43: ถ้า user ไม่เป็น None (แปลว่าหาเจอ)
if user is not None:
    
    # บรรทัด 44: เข้าสู่ระบบ (สร้าง session)
    login(request, user)
    
    # บรรทัด 45: บอกว่ายินดีต้อนรับ
    messages.success(request, f'ยินดีต้อนรับ {user.get_full_name()}')
```

**อธิบายแบบง่าย:**
- `login(request, user)` = สร้าง "ตั๋ว" ให้คุณ
- ตั๋วนี้เก็บใน Cookie (เหมือนบัตรสมาชิก)
- ต่อไปนี้ถ้าคุณเข้าเว็บอีก ระบบจะรู้ว่าคุณคือใคร

**Session ทำงานยังไง:**
```
1. ระบบสร้าง Session ID (เหมือนเลขที่บัตร) เช่น: "abc123xyz"
2. เก็บ Session ID ใน Cookie ของ browser
3. เก็บข้อมูล user ในฐานข้อมูล (django_session table)
4. ครั้งต่อไปที่เข้าเว็บ → ระบบดู Session ID → รู้ว่าคุณคือใคร
```

---

#### **ขั้นที่ 7: ส่งไปหน้าที่เหมาะสมตาม Role**

📁 **โค้ด:** `accounts/views.py` บรรทัด **48-53**

```python
# บรรทัด 48-49: ถ้าเป็น Admin → ไปหน้า course_list
if user.is_admin():
    return redirect('academic:course_list')

# บรรทัด 50-51: ถ้าเป็น Teacher → ไปหน้า dashboard
elif user.is_teacher():
    return redirect('teacher:dashboard')

# บรรทัด 52-53: ถ้าเป็น Student → ไปหน้า scan_page
else:
    return redirect('attendance:scan_page')
```

**อธิบายแบบง่าย:**
- ดูว่าคุณเป็นใคร (Admin, Teacher, หรือ Student)
- ส่งไปหน้าที่เหมาะสม:
  - Admin → หน้าจัดการรายวิชา
  - Teacher → หน้า dashboard ของอาจารย์
  - Student → หน้าสแกน QR Code

---

#### **ขั้นที่ 8: ถ้าไม่ถูกต้อง → แสดง Error**

📁 **โค้ด:** `accounts/views.py` บรรทัด **54-55**

```python
# บรรทัด 54-55: ถ้า user เป็น None (แปลว่าไม่เจอหรือ password ผิด)
else:
    messages.error(request, 'ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง')
```

**อธิบายแบบง่าย:**
- ถ้า username หรือ password ผิด → แสดงข้อความ error
- แสดงข้อความบนหน้า login ให้ user เห็น

---

## ✍️ หน้า Register (สมัครสมาชิก)

### 📍 โค้ดอยู่ที่ไหน?

| ส่วน | โฟลเดอร์ | ไฟล์ | บรรทัด | อธิบาย |
|------|---------|------|--------|--------|
| Template | `templates/accounts/` | `register.html` | 1-241 | หน้าเว็บที่ user เห็น |
| View Function | `accounts/` | `views.py` | 72-193 | โค้ดที่ประมวลผล |
| URL Config | `accounts/` | `urls.py` | 12 | เชื่อม URL กับ function |

---

### 🎨 หน้าจอ Register ทำงานยังไง? (แบบง่าย)

**เหมือนกับเราไปสมัครสมาชิกที่ร้าน:**
1. คุณเดินเข้าไป (เข้า URL: `/register/`)
2. คุณเห็นฟอร์มให้กรอกข้อมูล (หน้าเว็บแสดง)
3. คุณกรอกข้อมูลส่วนตัว (ชื่อ, อีเมล, รหัสผ่าน, ฯลฯ)
4. คุณส่งฟอร์มให้พนักงาน (กดปุ่ม "สมัครสมาชิก")
5. พนักงานเช็คว่าข้อมูลถูกต้องไหม (ระบบ validate)
6. ถ้าถูกต้อง → สร้างบัญชีให้ (บันทึกในฐานข้อมูล)
7. ถ้าไม่ถูกต้อง → บอกว่ามีปัญหา (แสดง error)

---

### 📝 ขั้นตอนการทำงาน (แบบละเอียด)

#### **ขั้นที่ 1: User เข้าหน้า Register**

📁 **โค้ด:** `templates/accounts/register.html` บรรทัด **1-241**

```
User พิมพ์: http://127.0.0.1:8000/register/
    ↓
Browser ส่ง request ไปยัง Server
    ↓
Server ตรวจสอบว่า user login อยู่แล้วหรือยัง?
```

**โค้ดที่ตรวจสอบ:**
```python
# accounts/views.py บรรทัด 77-78
if request.user.is_authenticated:
    return redirect('accounts:profile')  # ถ้า login อยู่แล้ว → ไปหน้า profile
```

---

#### **ขั้นที่ 2: แสดงหน้า Register พร้อมฟอร์ม**

📁 **โค้ด:** `templates/accounts/register.html` บรรทัด **77-185**

**HTML ที่แสดงบนหน้าจอ:**
```html
<!-- บรรทัด 77: เริ่มฟอร์มสมัครสมาชิก -->
<form method="post" action="{% url 'accounts:register' %}">
    
    <!-- บรรทัด 78: CSRF Token -->
    {% csrf_token %}
    
    <!-- บรรทัด 85: ช่องกรอกชื่อผู้ใช้ -->
    <input type="text" name="username" placeholder="กรอกชื่อผู้ใช้">
    
    <!-- บรรทัด 95: ช่องกรอกอีเมล -->
    <input type="email" name="email" placeholder="กรอกอีเมล">
    
    <!-- บรรทัด 105: ช่องกรอกชื่อ -->
    <input type="text" name="first_name" placeholder="กรอกชื่อ">
    
    <!-- บรรทัด 115: ช่องกรอกนามสกุล -->
    <input type="text" name="last_name" placeholder="กรอกนามสกุล">
    
    <!-- บรรทัด 126: ช่องกรอกรหัสนักศึกษา (ต้องกรอก) -->
    <input type="text" name="student_id" required placeholder="กรอกรหัสประจำตัว">
    
    <!-- บรรทัด 135: เลือกคณะ -->
    <select name="faculty_id">
        <option value="">-- เลือกคณะ --</option>
        ...
    </select>
    
    <!-- บรรทัด 149: เลือกสาขา -->
    <select name="major_id">
        <option value="">-- เลือกสาขา --</option>
        ...
    </select>
    
    <!-- บรรทัด 164: ช่องกรอกรหัสผ่าน -->
    <input type="password" name="password1" placeholder="กรอกรหัสผ่าน">
    
    <!-- บรรทัด 177: ช่องยืนยันรหัสผ่าน -->
    <input type="password" name="password2" placeholder="ยืนยันรหัสผ่าน">
    
    <!-- บรรทัด 184: ปุ่มกดเพื่อส่งข้อมูล -->
    <button type="submit">สมัครสมาชิก</button>
</form>
```

**อธิบายแบบง่าย:**
- มีช่องกรอกหลายช่อง
- `required` = ต้องกรอก (ถ้าไม่กรอกจะส่งไม่ได้)
- `<select>` = รายการให้เลือก (dropdown)

---

#### **ขั้นที่ 3: User กรอกข้อมูลแล้วกด Submit**

📁 **โค้ด:** `accounts/views.py` บรรทัด **80-89**

**เมื่อ user กดปุ่ม "สมัครสมาชิก":**
```python
# บรรทัด 80: ตรวจสอบว่าส่งข้อมูลแบบ POST มาไหม
if request.method == 'POST':
    
    # บรรทัด 81-87: ดึงข้อมูลทั้งหมดที่กรอกมา
    username = request.POST.get('username')
    email = request.POST.get('email')
    password1 = request.POST.get('password1')
    password2 = request.POST.get('password2')
    first_name = request.POST.get('first_name', '')
    last_name = request.POST.get('last_name', '')
    student_id = request.POST.get('student_id', '')
    
    # บรรทัด 89: กำหนด role เป็น student
    role = 'student'
```

**อธิบายแบบง่าย:**
- เก็บข้อมูลทั้งหมดที่ user กรอกมาไว้ในตัวแปร
- เหมือนเก็บกระดาษที่กรอกแล้วไว้ก่อน

---

#### **ขั้นที่ 4: ตรวจสอบข้อมูล (Validation)**

📁 **โค้ด:** `accounts/views.py` บรรทัด **91-103**

```python
# บรรทัด 92-93: เช็คว่าข้อมูลสำคัญครบไหม
if not username or not email or not password1 or not password2:
    messages.error(request, 'กรุณากรอกข้อมูลให้ครบถ้วน')

# บรรทัด 94-95: เช็คว่ามีรหัสนักศึกษาหรือไม่
elif not student_id:
    messages.error(request, 'กรุณากรอกรหัสนักศึกษา')

# บรรทัด 96-97: เช็คว่ารหัสผ่านตรงกันไหม
elif password1 != password2:
    messages.error(request, 'รหัสผ่านไม่ตรงกัน')

# บรรทัด 98-99: เช็คว่ารหัสผ่านยาวพอไหม (อย่างน้อย 8 ตัว)
elif len(password1) < 8:
    messages.error(request, 'รหัสผ่านต้องมีความยาวอย่างน้อย 8 ตัวอักษร')

# บรรทัด 100-101: เช็คว่าชื่อผู้ใช้มีคนใช้แล้วหรือยัง
elif User.objects.filter(username=username).exists():
    messages.error(request, 'ชื่อผู้ใช้นี้ถูกใช้งานแล้ว')

# บรรทัด 102-103: เช็คว่าอีเมลมีคนใช้แล้วหรือยัง
elif User.objects.filter(email=email).exists():
    messages.error(request, 'อีเมลนี้ถูกใช้งานแล้ว')
```

**อธิบายแบบง่าย:**
- ตรวจสอบทีละข้อว่า:
  1. กรอกข้อมูลครบไหม?
  2. มีรหัสนักศึกษาไหม?
  3. รหัสผ่านตรงกันไหม?
  4. รหัสผ่านยาวพอไหม?
  5. ชื่อผู้ใช้ซ้ำกับคนอื่นไหม?
  6. อีเมลซ้ำกับคนอื่นไหม?

**ฐานข้อมูลทำงานยังไง:**
```sql
-- ระบบจะ query ฐานข้อมูลแบบนี้:
SELECT * FROM accounts_user 
WHERE username = 'username_ที่กรอก';
```

---

#### **ขั้นที่ 5: เช็คว่ารหัสนักศึกษาเคยใช้แล้วหรือยัง**

📁 **โค้ด:** `accounts/views.py` บรรทัด **108-177**

```python
# บรรทัด 109: ไปดูในฐานข้อมูลว่ามีรหัสนักศึกษานี้ไหม
existing_profile = UserProfile.objects.filter(student_id=student_id).first()

# บรรทัด 111: ถ้ามี profile ที่มีรหัสนักศึกษานี้
if existing_profile and existing_profile.user:
    
    # บรรทัด 114: เช็คว่าเป็น placeholder user (จาก Excel) หรือเปล่า
    if existing_profile.user.username.startswith('student_'):
        # ถ้าใช่ → อัพเดตข้อมูลแทนการสร้างใหม่ (บรรทัด 119-150)
        # (อธิบาย: อาจารย์อาจโหลดข้อมูลจาก Excel มาแล้ว)
    else:
        # ถ้าไม่ใช่ → บอกว่ารหัสนักศึกษานี้ถูกใช้แล้ว
        messages.error(request, 'รหัสนักศึกษานี้ถูกใช้งานแล้ว')
```

**อธิบายแบบง่าย:**
- ระบบเช็คว่ารหัสนักศึกษานี้เคยมีคนใช้แล้วหรือยัง
- ถ้ามีแล้ว → บอกว่าใช้ไม่ได้
- ถ้ายังไม่มี → ไปขั้นตอนต่อไป

---

#### **ขั้นที่ 6: สร้าง User ใหม่**

📁 **โค้ด:** `accounts/views.py` บรรทัด **179-187**

```python
# บรรทัด 180-187: สร้าง User ใหม่
user = User.objects.create_user(
    username=username,      # ชื่อผู้ใช้
    email=email,            # อีเมล
    password=password1,     # รหัสผ่าน (จะถูก hash อัตโนมัติ)
    first_name=first_name,  # ชื่อ
    last_name=last_name,    # นามสกุล
    role=role               # บทบาท (student)
)
```

**อธิบายแบบง่าย:**
- สร้างบัญชีใหม่ในฐานข้อมูล
- `create_user()` = ฟังก์ชันที่สร้าง user + hash password ให้อัตโนมัติ

**ฐานข้อมูลทำงานยังไง:**
```sql
-- ระบบจะ insert ข้อมูลลงฐานข้อมูลแบบนี้:
INSERT INTO accounts_user (
    username, email, password, first_name, last_name, role, ...
) VALUES (
    'username', 'email@example.com', 'hashed_password', 'ชื่อ', 'นามสกุล', 'student', ...
);
```

---

#### **ขั้นที่ 7: สร้าง Profile (ข้อมูลเพิ่มเติม)**

📁 **โค้ด:** `accounts/views.py` บรรทัด **189-194**

```python
# บรรทัด 191-194: สร้าง Profile สำหรับ Student
profile = UserProfile.objects.create(
    user=user,              # เชื่อมกับ user ที่สร้างไว้
    student_id=student_id,  # รหัสนักศึกษา
)
```

**อธิบายแบบง่าย:**
- สร้าง Profile (ข้อมูลเพิ่มเติม) สำหรับ Student
- Profile = ข้อมูลเพิ่มเติมที่ไม่ได้อยู่ใน User table

**ฐานข้อมูลทำงานยังไง:**
```sql
-- ระบบจะ insert ข้อมูลลงฐานข้อมูลแบบนี้:
INSERT INTO accounts_userprofile (
    user_id, student_id, ...
) VALUES (
    1, '68342110008-1', ...
);
```

---

#### **ขั้นที่ 8: เชื่อมกับ Enrollment (ถ้ามี)**

📁 **โค้ด:** `accounts/views.py` บรรทัด **196-219**

```python
# ถ้ามี Enrollment ที่รออยู่ (pending) → อัพเดตให้เป็น enrolled
pending_enrollments = Enrollment.objects.filter(
    student__profile__student_id=student_id,
    status='pending'
)

if pending_enrollments.exists():
    # อัพเดตให้ student นี้เป็น enrolled
    for enrollment in pending_enrollments:
        enrollment.student = user
        enrollment.status = 'enrolled'
        enrollment.save()
```

**อธิบายแบบง่าย:**
- ถ้าอาจารย์โหลดข้อมูลจาก Excel มาแล้ว
- ระบบจะเชื่อม Student กับ Enrollment ที่รออยู่

---

#### **ขั้นที่ 9: แสดงข้อความสำเร็จและ Redirect**

📁 **โค้ด:** `accounts/views.py` บรรทัด **220-222**

```python
# บรรทัด 221: แสดงข้อความสำเร็จ
messages.success(request, 'สมัครสมาชิกสำเร็จ! กรุณาเข้าสู่ระบบ')

# บรรทัด 222: ส่งไปหน้า login
return redirect('accounts:login')
```

**อธิบายแบบง่าย:**
- บอกว่าสมัครสำเร็จแล้ว
- ส่งไปหน้า login ให้ user เข้าสู่ระบบ

---

## 💾 ฐานข้อมูลทำงานยังไง

### 📊 ตารางที่เกี่ยวข้อง

#### **1. ตาราง `accounts_user`**

📁 **โค้ด:** `accounts/models.py` บรรทัด **8-70**

**เก็บข้อมูลอะไรบ้าง:**
- `id` = เลขที่ (Primary Key)
- `username` = ชื่อผู้ใช้ (ต้อง unique)
- `password` = รหัสผ่าน (ถูก hash แล้ว)
- `email` = อีเมล
- `first_name` = ชื่อ
- `last_name` = นามสกุล
- `role` = บทบาท (admin, teacher, student)
- `is_active` = ใช้งานได้หรือไม่ (True/False)
- `date_joined` = วันที่สมัคร
- `last_login` = ครั้งล่าสุดที่ login

**ตัวอย่างข้อมูล:**
```
id | username  | password (hashed)           | role    | first_name | last_name
---|-----------|-----------------------------|---------|------------|-----------
1  | admin     | pbkdf2_sha256$...$abc123    | admin   | Admin      | User
2  | teacher1  | pbkdf2_sha256$...$def456    | teacher | วิมลศรี    | เกตุโสภณ
3  | student1  | pbkdf2_sha256$...$ghi789    | student | กิตติพัฒน์  | โสระมรรค
```

---

#### **2. ตาราง `accounts_userprofile`**

📁 **โค้ด:** `accounts/models.py` บรรทัด **73-159**

**เก็บข้อมูลอะไรบ้าง:**
- `id` = เลขที่ (Primary Key)
- `user_id` = Foreign Key → accounts_user.id (เชื่อมกับ user)
- `student_id` = รหัสนักศึกษา (สำหรับ Student)
- `teacher_employee_id` = รหัสอาจารย์ (สำหรับ Teacher)
- `student_major` = สาขา (สำหรับ Student)

**Relationship:**
```
accounts_user (1) ────── (1) accounts_userprofile
     ↓                           ↓
  1 user                  มี 1 profile
```

**ตัวอย่างข้อมูล:**
```
id | user_id | student_id    | student_major
---|---------|---------------|---------------
1  | 3       | 68342110008-1 | Computer Science
```

---

#### **3. ตาราง `django_session`**

**เก็บข้อมูลอะไรบ้าง:**
- `session_key` = Session ID (เช่น "abc123xyz")
- `session_data` = ข้อมูล session (encoded)
- `expire_date` = วันที่หมดอายุ

**ตัวอย่างข้อมูล:**
```
session_key  | session_data        | expire_date
-------------|---------------------|-------------
abc123xyz    | encoded_data...     | 2025-01-06
```

---

### 🔍 Query ที่เกิดขึ้นระหว่าง Login

#### **1. Authentication Query**

📁 **เรียกจาก:** `accounts/views.py` บรรทัด **42** (`authenticate()`)

```sql
SELECT * FROM accounts_user 
WHERE username = 'admin' 
AND is_active = TRUE;
```

**อธิบายแบบง่าย:**
- ไปหาว่ามี username นี้ไหม?
- และ is_active = TRUE (ใช้งานได้)

---

#### **2. Password Verification**

📁 **เรียกจาก:** Django Framework (`django.contrib.auth.hashers`)

```python
# Django ใช้ Python เปรียบเทียบ hash
check_password(raw_password, stored_hash)
```

**อธิบายแบบง่าย:**
- Hash password ที่ user กรอกมา
- เปรียบเทียบกับ hash ที่เก็บในฐานข้อมูล

---

#### **3. Session Creation**

📁 **เรียกจาก:** `accounts/views.py` บรรทัด **44** (`login()`)

```sql
INSERT INTO django_session (session_key, session_data, expire_date)
VALUES ('abc123xyz', 'encoded_data...', '2025-01-06 12:00:00');
```

**อธิบายแบบง่าย:**
- สร้าง session record ใหม่ในฐานข้อมูล
- เก็บ Session ID ไว้ใน Cookie

---

### 🔍 Query ที่เกิดขึ้นระหว่าง Register

#### **1. เช็คว่า Username ซ้ำไหม**

📁 **เรียกจาก:** `accounts/views.py` บรรทัด **100**

```sql
SELECT * FROM accounts_user 
WHERE username = 'username_ที่กรอก';
```

---

#### **2. เช็คว่า Email ซ้ำไหม**

📁 **เรียกจาก:** `accounts/views.py` บรรทัด **102**

```sql
SELECT * FROM accounts_user 
WHERE email = 'email@example.com';
```

---

#### **3. เช็คว่า Student ID ซ้ำไหม**

📁 **เรียกจาก:** `accounts/views.py` บรรทัด **109**

```sql
SELECT * FROM accounts_userprofile 
WHERE student_id = '68342110008-1';
```

---

#### **4. สร้าง User ใหม่**

📁 **เรียกจาก:** `accounts/views.py` บรรทัด **180**

```sql
INSERT INTO accounts_user (
    username, email, password, first_name, last_name, role, ...
) VALUES (
    'username', 'email@example.com', 'hashed_password', 'ชื่อ', 'นามสกุล', 'student', ...
);
```

---

#### **5. สร้าง Profile ใหม่**

📁 **เรียกจาก:** `accounts/views.py` บรรทัด **191**

```sql
INSERT INTO accounts_userprofile (
    user_id, student_id, ...
) VALUES (
    1, '68342110008-1', ...
);
```

---

## 📋 ตารางสรุปตำแหน่งโค้ดทั้งหมด

| ส่วน | โฟลเดอร์ | ไฟล์ | บรรทัด | คำอธิบาย |
|------|---------|------|--------|----------|
| **Login - URL** |
| Root URL | `checkin_project/` | `urls.py` | 11 | Include accounts URLs |
| Login URL | `accounts/` | `urls.py` | 11 | Map `/login/` |
| **Login - View** |
| Login View | `accounts/` | `views.py` | 29-59 | Main login logic |
| Check Auth | `accounts/` | `views.py` | 34-35 | Check if logged in |
| Extract Data | `accounts/` | `views.py` | 38-39 | Get username/password |
| Authenticate | `accounts/` | `views.py` | 42 | Verify credentials |
| Create Session | `accounts/` | `views.py` | 44 | Login user |
| Role Redirect | `accounts/` | `views.py` | 48-53 | Redirect by role |
| Error Message | `accounts/` | `views.py` | 54-55 | Show error |
| **Login - Template** |
| Login HTML | `templates/accounts/` | `login.html` | 1-127 | Full page |
| Form | `templates/accounts/` | `login.html` | 76-100 | Login form |
| CSRF Token | `templates/accounts/` | `login.html` | 77 | Security |
| Username Input | `templates/accounts/` | `login.html` | 84 | Username field |
| Password Input | `templates/accounts/` | `login.html` | 93 | Password field |
| Password Toggle | `templates/accounts/` | `login.html` | 108-123 | Show/hide JS |
| **Register - URL** |
| Register URL | `accounts/` | `urls.py` | 12 | Map `/register/` |
| **Register - View** |
| Register View | `accounts/` | `views.py` | 72-193 | Main register logic |
| Check Auth | `accounts/` | `views.py` | 77-78 | Check if logged in |
| Extract Data | `accounts/` | `views.py` | 81-87 | Get all form data |
| Validation | `accounts/` | `views.py` | 91-103 | Validate inputs |
| Check Student ID | `accounts/` | `views.py` | 109 | Check if exists |
| Create User | `accounts/` | `views.py` | 180-187 | Create new user |
| Create Profile | `accounts/` | `views.py` | 191-194 | Create profile |
| Success Message | `accounts/` | `views.py` | 221-222 | Show success |
| **Register - Template** |
| Register HTML | `templates/accounts/` | `register.html` | 1-241 | Full page |
| Form | `templates/accounts/` | `register.html` | 77-185 | Register form |
| Username Input | `templates/accounts/` | `register.html` | 85 | Username field |
| Email Input | `templates/accounts/` | `register.html` | 95 | Email field |
| Student ID Input | `templates/accounts/` | `register.html` | 126 | Student ID field |
| Password Toggle | `templates/accounts/` | `register.html` | 193-238 | JS functions |
| **Models** |
| User Model | `accounts/` | `models.py` | 8-70 | User class |
| Profile Model | `accounts/` | `models.py` | 73-159 | Profile class |
| **Settings** |
| Auth User Model | `checkin_project/` | `settings.py` | 151 | AUTH_USER_MODEL |
| Login URL | `checkin_project/` | `settings.py` | 154 | LOGIN_URL |

---

## 🎓 สรุปแบบสั้นๆ

### **หน้า Login:**
1. User เข้าหน้า login → แสดงฟอร์ม
2. User กรอก username/password → ส่งข้อมูล
3. ระบบเช็คในฐานข้อมูล → ถูกต้องหรือไม่?
4. ถ้าถูกต้อง → สร้าง session → redirect ไปหน้าที่เหมาะสม
5. ถ้าไม่ถูกต้อง → แสดง error

### **หน้า Register:**
1. User เข้าหน้า register → แสดงฟอร์ม
2. User กรอกข้อมูลทั้งหมด → ส่งข้อมูล
3. ระบบตรวจสอบข้อมูล → ถูกต้องครบถ้วนหรือไม่?
4. ระบบเช็คว่าซ้ำหรือไม่ → username, email, student_id
5. ถ้าผ่านทั้งหมด → สร้าง User + Profile → แสดง success
6. Redirect ไปหน้า login

---

**สร้างเมื่อ:** 2025-01-05  
**อัปเดตล่าสุด:** 2025-01-05















